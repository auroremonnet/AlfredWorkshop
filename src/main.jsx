import React from "react";
import ReactDOM from "react-dom/client";
import AlfredWorkspace from "./AlfredWorkspace.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <AlfredWorkspace />
  </React.StrictMode>
);
